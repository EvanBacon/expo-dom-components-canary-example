import RTFShoes from "@/components/shoes";

export default function App() {
  return (
    <RTFShoes
      style={{
        height: "100vh",
      }}
      dom={{
        scrollEnabled: false,
      }}
    />
  );
}
